//import cors from 'cors'

export default (app)=>{


/*    app.ws('/activities/signin/:id',cors(),function(ws, req, next){
        ws.on('activities_signin_room', function(msg) {
            console.log(msg);
        });
    })*/
}